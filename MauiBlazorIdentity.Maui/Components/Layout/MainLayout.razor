@inherits LayoutComponentBase

<div class="page">
    <div class="sidebar">
        <NavMenu />
    </div>

    <main>
        <div class="top-row px-4">
            <AuthorizeView>
                <Authorized>
                    <span>Bonjour, @context.User.Identity?.Name!</span>
                    <a href="/profile" class="ms-3">Mon profil</a>
                </Authorized>
                <NotAuthorized>
                    <a href="/login">Se connecter</a>
                </NotAuthorized>
            </AuthorizeView>
        </div>

        <article class="content px-4">
            @Body
        </article>
    </main>
</div>
